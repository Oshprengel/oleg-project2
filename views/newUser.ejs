<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/loginStyle.css">
    <title>Create New User</title>
</head>
<body>
    <!-- If there is an error currently in the session then post it here -->
    <h1>New User 👋</h1>
    <!-- new user form -->
    <div id='background'>
    <div id='login'>
    <form action="/users" method="POST">
        <% if(typeof error !== 'undefined'){%>
            <h2><%=error%></h2>
        <%}%>
        <!-- Username input -->
        <input type="text" name="name" placeholder="User Name" required="required">
        <!-- Password input -->
        <input type="password" name="password" placeholder="Password" required="required"></label>
        <!-- Submit button -->
        <input type="submit" value="Create New User">
        <!-- back to login link -->
        <a href="/users/login"><button type="button">Back To Login</button></a>
    </form>

    </div>
</div>
</body>
</html>