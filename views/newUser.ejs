<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New User</title>
</head>
<body>
    <!-- If there is an error currently in the session then post it here -->
    <% if(typeof error !== 'undefined'){%>
        <%=error%>
    <%}%>
    <!-- new user form -->
    <form action="/users" method="POST">
        <!-- Username input -->
        <input type="text" name="name" placeholder="User Name" required="required">
        <!-- Password input -->
        <input type="password" name="password" placeholder="Password" required="required"></label>
        <!-- Submit button -->
        <input type="submit">
    </form>
    <a href="/users/login"><button>Back To Login</button></a>
</body>
</html>